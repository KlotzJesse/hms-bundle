import { Editor } from '@tldraw/tldraw';
export declare const useSetEditorPermissions: ({ token, editor, zoomToContent, handleError, }: {
    token?: string;
    editor?: Editor;
    zoomToContent?: boolean;
    handleError: (error: Error) => void;
}) => any;
