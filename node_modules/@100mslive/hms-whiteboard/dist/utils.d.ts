export default function decodeJWT(token?: string): any;
export interface BackoffState {
    attempt: number;
    currentDelay: number;
}
export declare const calculateBackoff: (state: BackoffState) => number;
