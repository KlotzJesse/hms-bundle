import React from 'react';
import './index.css';
export interface WhiteboardProps {
    endpoint?: string;
    token: string;
    zoomToContent?: boolean;
    transparentCanvas?: boolean;
    onMount?: (args: {
        store?: unknown;
        editor?: unknown;
    }) => void;
}
export declare function Whiteboard(props: WhiteboardProps): React.JSX.Element;
